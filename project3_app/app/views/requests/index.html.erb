
√
 <div class = "informtion">
  <% @requests.each do |item| %>
    <% if item.explainsituation.to_i == nil ||item.note.to_i == nil ||item.age== nil %>

    <%else%>
<figure class="snip1336">
  
  <figcaption>
    <h2>Name of Caredom <%= item.gender %> <span>Chaild</span></h2>
  
     the Caredom has a number of children: <%= item.explainsituation.to_i  + item.age + item.note.to_i  %> 
     </br>
     Age:
      </br>
    0 to 6 months : <%= item.age%>
 </br>
    6 months to 3 years : <%=item.explainsituation %>
     </br>
    3 to 7 years : <%=item.note %> 
</br>
<% data= item.date.to_s.split(" ")%>
in a day :<%= data[0]%>
</br>
allergies : <%= item.allergies %>
</br>

In the hour: <%= data[1]%> PM
</p>

    <p>   </p>
  
<% if item.request_status == 0%>

    <a class="follow foo" id="<%=item.id %>">Request </a>
   
    <% elsif item.request_status == 1 %>
    <a  class="follow bool" id="<%=item.id %>">hanging ⏰</a>
<% else%>
    <a class="follow sool">taken </a>

    <% end %>

  </figcaption>
</figure>
<%end%>
<% end %>

</div>